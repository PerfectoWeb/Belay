Quality-of-life update.

- The menu bar mark now tells you when an update is out, and stays quiet while an agent is working
- One notification per version. Open it, see what changed, update when you feel like it
- If a session stops without finishing, Belay says so instead of pretending it finished
- Crash reports can be kept on your Mac now, off by default, nothing sent anywhere
- Skip This Version is gone. It was a nudge, and the mark is already easy to ignore

Thanks to @VenutNSA for the docs fix and for digging into lid-close sleep.

brew install --cask belay
