- **A rare overnight crash is fixed.** With Keep local reports switched on, the diagnostics log's sink grew one closure wrapper heavier with every line written — after a night of steady agent traffic the stored callback was thousands of frames deep, and the next write overflowed the stack and took Belay down without a crash dialog. Diagnosed to the exact frame from a user's `.ips` report, fixed structurally, and guarded by a regression test. Thank you for the precise report.
- **Set the timer by end time.** The custom Always On timer now takes a length ("For") or a wall-clock time ("Until") — a time already past means tomorrow — and a footnote always shows the other reading as you type. The dialog rides the panel as a sheet instead of opening behind it.
- **Take your statistics with you.** The erase dialog offers a CSV export of every recorded day before anything is deleted, and the Statistics headline flips to your at-the-keyboard time on hover.

Also: chart bars trade their weekday for the real date on hover, switched-off agent tiles dim as one piece, the Off pill sits back in dark mode, the statistics card attaches properly when sharing to Telegram and friends, Cline custom folders under `/tmp`-style paths detect correctly, and a Belay that dies without saying goodbye is named in the log on the next launch.
