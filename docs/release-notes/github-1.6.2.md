- **Long-running commands now keep your Mac awake.** Belay follows silent test runs, builds, and other long tool calls until they actually finish. Works with Precise Detection and with process-based detection. Reported by a user whose 20–30 minute test runs kept losing the Mac.

- **Longer sleep delays are one Shift away.** Hold Shift in the Sleep delay menu to reveal 15 minutes, 30 minutes, and 1 hour. Your choice stays visible afterwards.

- **Review Precise Detection without removing it.** An agent’s menu now lets you open **Rules…**, update them in place, or **Turn Off** detection.

Also: repeated log messages now back off instead of flooding diagnostics, and local ad-hoc builds launch correctly again.