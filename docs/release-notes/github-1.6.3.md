- **Precise Detection survives restarts and updates.** Belay now keeps the same local port whenever possible, so agents stay connected across relaunches and Sparkle updates instead of hitting `ECONNREFUSED`.

- **Bridge diagnostics are finally useful.** The log now records when the local bridge starts, which port it uses, retries, failures, and settings files it updates.

- **Option reveals the longer choices too.** Hold Option or Shift in the Sleep delay menu or Always On timer to show the longer durations.