#!/usr/bin/env bash
# Refuses a commit that would add a second author to a tracked file.
# Enable with:  git config core.hooksPath .githooks
exec "$(git rev-parse --show-toplevel)/scripts/one-author.sh" --staged
